/// 应用配置
class AppConfig {
  /// 控制面板服务器默认IP地址
  static const String defaultServerIp = '';
  
  /// 控制面板服务器默认端口
  static const int defaultServerPort = 8888;
  
  /// 是否自动连接服务器（默认不自动连接）
  static const bool defaultAutoConnect = false;
}
